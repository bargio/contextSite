type Quiz @model {
  id: ID!
  name: String!
  description: String
  image_url: String
  active: Boolean
  quizQuestionsID: String
}

type QuizQuestions @model {
  id: ID!
  quizDetails: String!
}

type QuizResult @model {
  id: ID!
  quizId: String!
  quizUser: String!
  quizResult: Int!
  quizID: String
}

